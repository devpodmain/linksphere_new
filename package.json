{
  "name": "linksphere",
  "version": "1.0.0",
  "description": "A web application for creating public digital profiles with links, bios, collaborations, analytics, QR codes, subscriptions, and bookings",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && php -S localhost:8000 -t public",
    "build": "cd frontend && npm run build",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && composer install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}


